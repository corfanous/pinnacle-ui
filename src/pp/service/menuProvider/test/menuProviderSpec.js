describe('menuProvider test',function(){
	var _menuProvider;
	//load the module
	beforeEach(module('pinnacle.ui'));
	//inject the service
	beforeEach(inject(function(_menuProvider_){
		_menuProvider=_menuProvider_;
	}));
	
	it('test code ',function(){
		
	});
});
