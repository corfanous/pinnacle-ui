'use strict'
/**
 * @name: pHeader 
 */
/*angular.module('pinnacle.ui')
.directive('pHeader',['basePanel',function(bp){
	return {
		restrict:'A',
		scope:{
			name:'@',
			visible:'@',
			panelClass:'@'
		},
		compile:function(elem,attrs){
			bp.setupTemplate(elem,attrs);
			pre:function(scope,elem,attr){
				bp.setupPre(scope,elem,attr);
			},
			post:function(scope,elem,attr,ctrl,transclude){
				bp.setupPost(scope,elem,attr,ctrl,transclude);
			}
		}
	}
}]);*/
