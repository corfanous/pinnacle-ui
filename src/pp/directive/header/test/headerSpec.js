/*describe('pHeader test',function(){
	var scope,$compile,$templateCache,$controller,elem;
	//load the pinnacleUi module
	beforeEach(module('pinnacle.ui'));
	//load the template
	//beforeEach(module('test/header.html'));
	//inject dependencies
	beforeEach(inject(function(_$rootScope_, _$compile_, _$templateCache_, _$controller_){
		scope=_$rootScope_;
		$compile=_$compile_;
		$templateCache=_$templateCache_;
		$controller=_$controller_;
	}));
	describe('pHeader controller test',function(){
		beforeEach(inject(function(){
			elem=$compile("<div p-header></div>")(scope)
			scope.$digest();
		}))
		it('test the scope properties',function(){
			//expect()
		});
	});
	xdescribe('pHeader template test',function(){
    	it('actual test code',function(){
    		
    	});
	});
});*/

